package:
  name: xentropy
  version: "3.1a"

source:
  path: .

requirements:
  build:
    - fftw
    - python
    - intel-openmp
    - {{ compiler('cxx') }}
  
  host:
    - python
    - setuptools
    - Cython
    - fftw
    - intel-openmp
  
  run:
    - numpy

test:
  imports:
    - xentropy
